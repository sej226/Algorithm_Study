풀이과정
인자 N을 2진수로 변환
1의 갯수를 확인
10진수 N < x <= 1,000,000 사이의 숫자를 하나씩 2진수로 바꾸어 1의 갯수가 위의 2와 같다면, x를 리턴한다.
배운점
bin(), oct(), hex() 내장함수를 통해 정수를 각 2진수, 8진수, 16진수로 바꿀 수 있다.
str() 내장함수를 통해 인자를 문자열로 변환한다.
str.count(‘검색어’)를 통해서 해당 검색어의 갯수를 리턴한다.